<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- pressed state: golden yellow for a nice highlight -->
    <item android:color="@color/golden_yellow" android:state_pressed="true" />
    <!-- default state: white so numbers are readable on dark buttons -->
    <item android:color="@android:color/white" />
</selector>