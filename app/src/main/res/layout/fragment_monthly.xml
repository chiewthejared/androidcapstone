<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Month and Year Header -->
    <LinearLayout
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp">

        <Button
            android:id="@+id/prevMonthButton"
            android:text="Prev"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/monthYearText"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="March 2025"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/nextMonthButton"
            android:text="Next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- Days of the Week Header -->
    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="7"
        android:rowCount="1"
        android:layout_marginBottom="4dp">

        <!-- Repeat for all 7 days -->
        <TextView android:text="Sun" style="@style/DayHeaderStyle"/>
        <TextView android:text="Mon" style="@style/DayHeaderStyle"/>
        <TextView android:text="Tue" style="@style/DayHeaderStyle"/>
        <TextView android:text="Wed" style="@style/DayHeaderStyle"/>
        <TextView android:text="Thu" style="@style/DayHeaderStyle"/>
        <TextView android:text="Fri" style="@style/DayHeaderStyle"/>
        <TextView android:text="Sat" style="@style/DayHeaderStyle"/>
    </GridLayout>

    <!-- Calendar Grid -->
    <GridView
        android:id="@+id/calendarGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:numColumns="7"
        android:verticalSpacing="8dp"
        android:horizontalSpacing="8dp"/>
</LinearLayout>
